<UserControl x:Class="BOOK_SCRIBBLE_PROJECT.Views.ShelfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:BOOK_SCRIBBLE_PROJECT.ViewModels"
             xmlns:views="clr-namespace:BOOK_SCRIBBLE_PROJECT.Views"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance viewmodels:ShelfViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <ItemsControl ItemsSource="{Binding Books}" Grid.Row="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <views:BookView/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Grid.Row="1" Background="#5F4B3C" CornerRadius="0,0,10,10"
                Margin="10,0,10,0" BorderBrush="#000" BorderThickness="1"/>
    </Grid>
</UserControl>