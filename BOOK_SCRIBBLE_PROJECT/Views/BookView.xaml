<UserControl x:Class="BOOK_SCRIBBLE_PROJECT.Views.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:BOOK_SCRIBBLE_PROJECT.ViewModels"
             xmlns:converters="clr-namespace:BOOK_SCRIBBLE_PROJECT.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="130" d:DesignWidth="90"
             d:DataContext="{d:DesignInstance viewmodels:BookViewModel}">

    <UserControl.Resources>
        <converters:ImagePathToBitmapConverter x:Key="PathToBitmapConverter"/>
    </UserControl.Resources>

    <Image Source="{Binding CoverImage, Converter={StaticResource PathToBitmapConverter}}"
           Width="80"
           Height="100"
           Stretch="UniformToFill"
           RenderOptions.BitmapScalingMode="HighQuality"
           Margin="5"/>
</UserControl>