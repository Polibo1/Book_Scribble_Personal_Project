<UserControl x:Class="BOOK_SCRIBBLE_PROJECT.UserControls.MainLayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:usercontrols="clr-namespace:BOOK_SCRIBBLE_PROJECT.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="875" d:DesignWidth="500"
             FontFamily="Noto Sans KR">
    <Grid Background="#FFF2CC">
        <Grid.RowDefinitions>
            <RowDefinition Height="46"/>
            <RowDefinition Height="26"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10" Grid.RowSpan="2">
            <Image Source="/Assets/CHARACTER.png" Width="40" Height="40" RenderOptions.BitmapScalingMode="HighQuality"/>
            <Image Source="/Assets/TITLE.png" Height="52" Width="100"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="10" Background="#FFFFFF" BorderBrush="#000" BorderThickness="1" Margin="20,10,32,10" Panel.ZIndex="1">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="5" Opacity="0.3" BlurRadius="5"/>
                </Border.Effect>
            </Border>

            <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right" Margin="-50,60,10,10" Panel.ZIndex="2">
                <StackPanel.Resources>
                    <Style x:Key="TabButtonStyle" TargetType="ToggleButton">
                        <Setter Property="Width" Value="40"/>
                        <Setter Property="Height" Value="100"/>
                        <Setter Property="Margin" Value="0,0,0,30"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Background" Value="#FFFFFF"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderBrush" Value="#C0D5A3"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="0,5,5,0">
                                        <TextBlock Text="{TemplateBinding Content}"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"
                                                   Foreground="{TemplateBinding Foreground}">
                                            <TextBlock.LayoutTransform>
                                                <RotateTransform Angle="90"/>
                                            </TextBlock.LayoutTransform>
                                        </TextBlock>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#009800"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Resources>

                <ToggleButton x:Name="BtnList" Content="도서목록" Style="{StaticResource TabButtonStyle}" IsChecked="True"/>
                <ToggleButton x:Name="BtnTower" Content="도서타워" Style="{StaticResource TabButtonStyle}" Checked="BtnTower_Checked"/>

            </StackPanel>

            <Border Grid.Column="0" Margin="10,0,40,20" CornerRadius="10" Background="#EEFACF" BorderBrush="#000" BorderThickness="1" 
                    RenderTransformOrigin="0.5,0.5" Panel.ZIndex="3">
                <Border.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleY="0.178" AngleX="0.025"/>
                        <RotateTransform/>
                        <TranslateTransform Y="0.748" X="0.171"/>
                    </TransformGroup>
                </Border.RenderTransform>
                <Border.LayoutTransform>
                    <TranslateTransform X="-5" Y="-5"/>
                </Border.LayoutTransform>

                <!-- Host for swappable views (BookShelfBackgroundView / BookTowerView) -->
                <Border.Child>
                    <ContentControl x:Name="ScrollContentHost"/>
                </Border.Child>

            </Border>
        </Grid>
    </Grid>
</UserControl>